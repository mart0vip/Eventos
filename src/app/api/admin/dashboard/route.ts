import { NextResponse } from "next/server";

// FASE 4: Dashboard Tesorería — scaffolded, not implemented in Fase 1.
export async function GET() {
  return NextResponse.json({ error: "not_implemented", phase: "FASE 4" }, { status: 501 });
}
